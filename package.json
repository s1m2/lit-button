{
  "name": "lit-button",
  "version": "0.0.1",
  "description": "A web component button built from lit",
  "main": "main.ts",
  "scripts": {
    "start": "web-dev-server --open ./ --node-resolve --watch",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "sam macharia",
  "license": "MIT",
  "dependencies": {
    "lit": "^2.4.1"
  },
  "devDependencies": {
    "@open-wc/testing": "^3.0.0-next.5",
    "@web/dev-server": "^0.1.35",
    "eslint": "^8.27.0",
    "lit-analyzer": "^1.2.1",
    "typescript": "^4.8.4"
  }
}
